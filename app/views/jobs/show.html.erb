<p id="notice"><%= notice %></p>

<iframe src="https://appear.in/half-up-front/<%= @job.id %>" width="400" height="480" frameborder="0"></iframe>

<h3>Welcome <%= current_user.first_name %> </h3>

<div>
<%= @milestone1.name %>
  <div>
    <%= @requirement1.first.name %>
    <%= @requirement1.first.details %>
    <%= @requirement1.first.renegotiable %>
    <%= @requirement1.first.milestone_id %>
  </div>
</div>
<div>
<%= @milestone2.name %>
  <div>
    <%= @requirement2 %>
  </div>
</div>


<%= @job.inspect %>

<%= link_to 'Edit', edit_job_path(@job) %> |
<%= link_to 'Back', jobs_path %>

**THIS WILL SHOW A JOB**